#/bin/sh

#PGDATA=/usr/local/pgsql/data
#PGLIB=/usr/lib/pgsql

PATH=$PATH:$HOME/cibh/scripts

for NETWORK in $*
do
  export NETWORK
  snmp-poll-all $HOME/data/$NETWORK/routers 
  usage2fig `ls $HOME/data/$NETWORK/figs/*.fig`
done

