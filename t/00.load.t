use Test::More tests => 7;

use_ok( 'CIBH' );
use_ok( 'CIBH::Chart' );
use_ok( 'CIBH::Datafile' );
use_ok( 'CIBH::Dia' );
use_ok( 'CIBH::Fig' );
use_ok( 'CIBH::Win' );
use_ok( 'CIBH::Graphviz' );

diag( "Testing CIBH $CIBH::VERSION" );
