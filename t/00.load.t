use Test::More tests => 9;

use_ok( 'CIBH' );
use_ok( 'CIBH::Chart' );
use_ok( 'CIBH::Datafile' );
use_ok( 'CIBH::Dia' );
use_ok( 'CIBH::Fig' );
use_ok( 'CIBH::Win' );
use_ok( 'CIBH::Graphviz' );
use_ok( 'CIBH::Logs' );
use_ok( 'CIBH::Config' );

diag( "Testing CIBH $CIBH::VERSION" );
